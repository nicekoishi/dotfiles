{
  nice.host.services = {
    # TODO: Use forgejo for authentication, add kanidm login option
    # This is a bit too advanced for the current me, go read a bit before coming back to this
    # kanidm.enable = true;
    # navidrome.enable = true;
  };
}

/* the good stuff?
This has to be on top if you want to use windows with widgets that have custom icons
Path resolution breaks with nix for some reason
*/
export default () => {
    App.addIcons("../assets");
};

{
    "name": "nicekoishi-ags",
    "author": "nicekoishi",
    "description": "Ags configuration, based on nyx-ags.",
    "scripts": {
        "lint": "eslint . --fix",
        "build": "pnpm run lint && compile-stylesheet && build-ags-config",
        "test": "pnpm run build && ags -c ./outputs/out/config.js"
    },
    "devDependencies": {
        "@typescript-eslint/eslint-plugin": "^8.18.2",
        "@typescript-eslint/parser": "^8.18.2",
        "eslint": "^9.17.0",
        "typescript": "^5.7.2"
    }
}
